<!DOCTYPE html>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<link rel="stylesheet" href="/css/votes.css">
<a href="/"><button class="btn btn-info">Home</button></a><br>
<blockquote>
	<p>
		<a href="{{link.url}}"> {{link.title}}</a>
	</p>
	<footer>BY: {{link.username}}</footer>
</blockquote>
<form method="post">
	Sort Comments By:<input class="btn btn-info" type="submit" name="time" value="Time">
	<input class="btn btn-info" type="submit" name="vote" value="Vote">
</form>
{% for comment in comments %}
	
	<blockquote class="container-fluid">{{comment.content}}
	<footer>By: {{comment.username}}</footer>
	</blockquote>
	<form method="post" class="container-fluid" class="form-control">
		{{comment.vote}}
		{% if comment.voted == "1" %}
			<input type="submit" class="btn btn-primary active" name="upvote{{comment.id}}" value="upvote">  <!-- Ignore HTMLLintBear -->
			<input type="submit" class="btn btn-default" name="downvote{{comment.id}}" value="downvote">
		{% elseif comment.voted == "-1" %}
			<input class="btn btn-default" type="submit" name="upvote{{comment.id}}" value="upvote">
			<input class="btn btn-danger active" type="submit" name="downvote{{comment.id}}" value="downvote">
		{% else %}
			<input type="submit" class="btn btn-default" name="upvote{{comment.id}}" value="upvote">
			<input type="submit" class="btn btn-default" name="downvote{{comment.id}}" value="downvote">
		{% endif %}
	</form>
	<hr>
{% endfor %}
<div class="container" class="col-lg-1">
<form method="post">
	Your Comment:<input class="form-control" type="text" name="comment">
	<input class="btn btn-default" type="submit" name="submit" value="submit">
</form>
</div>