// static methods cannot be accesed using objects unlike Java
// composer.lock makes sure that the same versions are used by other developers & thinks donk break ;)
// Since namespaces can be nested it is better to use \ before the namespace :Bounced huun??
// Use composer dump-autoload when you make any changes  to the files that have been imported or to composer.json
// Instead of adding the dependency directly in composer.json we can also use the command composer require "blah-blah"
//For checking the error logs
tail -f /var/log/apache2/error.log //might want to use sudo
//there is also a file named access.log in this directory
//twig syntax to access a variable:
{{v1}}
